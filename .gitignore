# Logs
*.log

# Sistema / editor
.DS_Store
.idea
*.local

# Secretos locales (API keys) — *.local no matchea *.local.md, entrada explícita
SECRETS.local.md

# Backups de colecciones de addons (datos de cuenta, no versionar)
.backups

# Copia de staging para deployar deno-hub.ts aislado del resto del repo
# (evita subir SECRETS.local.md/.backups al acotar el root-path del deploy)
.deploy-hub
